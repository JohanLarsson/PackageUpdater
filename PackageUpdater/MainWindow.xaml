<Window
    x:Class="PackageUpdater.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PackageUpdater"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.DataContext>
        <local:ViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding GitDirectory}" />
            <Button
                Grid.Column="1"
                Command="{Binding BrowseForGitDirectoryCommand}"
                Content="..." />
        </Grid>
        <TextBox Grid.Row="1" Text="{Binding PackageId}" />

        <ListBox Grid.Row="2" ItemsSource="{Binding FilteredRepositories}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type local:Repository}">
                    <TextBlock Text="{Binding RootDirectory}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
